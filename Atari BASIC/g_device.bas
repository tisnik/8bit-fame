5 DIM A$(100):WAIT=1000:SEC=1
6 REM INIT ADDRESS OF G_DEVICE V.1.1
7 INIT=39179
8 ? "INIT G DEVICE";:INPUT A$:A$(LEN(A$)+1)=" "
9 IF A$(1,1)="Y" THEN A=USR(INIT)
11 DATA FJBBB,DJBDN,LBDBL,FJNBB
12 DATA NHLBL,FHLJF
13 ? "}WRITING RECORDS:"
14 OPEN #2,8,0,"G3:":? "REC ";
15 FOR I=1 TO 4:READ A$
16 ? I;" ";:? #2;" ";A$;" "
17 GOSUB WAIT:NEXT I
20 ? :? "CLOSING DEVICE"
30 CLOSE #2:GOSUB WAIT
31 ? :? :? "OPENING FOR READING"
35 ? "   AND COPY DATA TO ANOTHER ONE":? "COPY ";
40 OPEN #2,4,0,"G3:":OPEN #3,8,0,"G4:"
50 FOR I=1 TO 4
52 ? I;" ";:INPUT #2,A$:? #3;A$
54 GOSUB WAIT:NEXT I
56 CLOSE #2:CLOSE #3
60 GOSUB WAIT
61 ? :? "OPENING FOR READING BACK"
64 OPEN #2,4,0,"G4:"
70 FOR I=1 TO 4
72 INPUT #2,A$:? I;": '";A$;"'"
74 GOSUB WAIT:NEXT I
80 CLOSE #2:GOSUB WAIT
81 ? :? "OPENING TO APPEND DATA"
84 OPEN #2,9,0,"G3:"
90 FOR I=5 TO 6
92 READ A$:? I;" ";:? #2;" ";A$;" "
94 GOSUB WAIT:NEXT I
96 CLOSE #2:GOSUB WAIT
100 ? "}OPENING G4 FOR BOTH READING AND APPEND"
102 OPEN #2,13,0,"G4:"
104 TRAP 110:? "READING фиже ITEMS"
106 FOR I=1 TO 5:INPUT #2,A$:? I;" ";
108 GOSUB WAIT:NEXT I
110 ? :? "ERROR: ";PEEK(195)
112 ? :? "DEVICE STILL WORKS, ADDING DATA..."
114 RESTORE 12
120 FOR I=5 TO 6:READ A$:? I;" ";:? #2;" ";A$;" "
122 GOSUB WAIT:NEXT I
130 ? :? "WE CAN READ MORE нов"
132 FOR I=5 TO 6:INPUT #2,A$:? I;" ";
134 GOSUB WAIT:NEXT I
140 CLOSE #2:? "CLOSING DEVICE"
142 GOSUB WAIT
150 ? "...":GOSUB WAIT
152 ? "AS NEITHER DEVICE WAS REOPEN"
154 ? "FOR NEW WRITE,"
156 ? "DATA IS STILL THERE, IN BOTH ONES"
160 OPEN #2,4,0,"G3:":OPEN #3,4,0,"G4:"
162 SEC=2:GOSUB WAIT:GOTO 2000
999 END 
1000 FOR W=1 TO SEC*700:NEXT W:RETURN 
2000 CLOSE #2:CLOSE #3:OPEN #2,8,0,"G3:":CLOSE #2:OPEN #2,8,0,"G4:":CLOSE #2

